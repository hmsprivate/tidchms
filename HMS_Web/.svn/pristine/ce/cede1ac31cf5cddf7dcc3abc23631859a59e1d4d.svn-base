define([], function() {
    'use strict';
    
    var model = (function() {
        var pageIndex = 1;
        var pageCount = 20;
        
        //task order
        var taskOrder = '';
        var taskExp = '';
        
        var groupId = '';
        
        //task search
        var taskFileName = '';
        var taskFileContents = '';
        
        return {
	    	pageIndex: pageIndex,
	    	pageCount: pageCount,
	    	
	    	taskOrder: taskOrder,
	    	taskExp: taskExp,
	    	
	    	groupId: groupId,
	    	
	    	taskFileName: taskFileName,
	    	taskFileContents: taskFileContents
	    }
    });

    return model;
});